<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tpm.project">
	<insert id="projectInsert" parameterType="tpm.project.model.ProjectDTO">
		insert into tpm_project
		values(
			project_idx.nextval,
			#{project_name},
			#{project_content},
			1
		)
		
				
	</insert>
	
	<select id="projectSearch" parameterType="tpm.project.model.ProjectDTO" resultType="tpm.category.model.CategoryDTO">
	select * from tpm_category
	where project_idx=#{project_idx}
	</select>
</mapper>