<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tpm.project_member">
	<insert id="projectMemberInsert" parameterType="tpm.project.model.ProjectMemberDTO">
		insert into tpm_project_member
		values(
			#{project_idx},
			#{member_idx},
			#{project_level}
			)
	</insert>
	<select id="updateProjectFriend" parameterType="tpm.project.model.ProjectMemberDTO" resultType="tpm.member.model.MemberDTO">
	
		select m.* from
		(
			tpm_member
		)m,
		(
			select pm.member_idx from
			(select * from tpm_project_member where project_idx=#{project_idx} and member_idx!=#{member_idx})pm,
			(select * from tpm_myfriend where member_idx=#{member_idx})f
			where f.myfriend_idx!=pm.member_idx
		)aa
		where m.member_idx=aa.member_idx
	</select>
	
	<update id="projectMeberUpdate" parameterType="tpm.project.model.ProjectMemberDTO">
		update tpm_project_member
		set
		project_idx=#{project_idx},
		project_member_idx=#{project_member_idx},
		project_level=#{project_level}
		
	</update>
</mapper>