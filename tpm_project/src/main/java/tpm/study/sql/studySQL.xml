<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tpm.study">

	<!-- Mybatis DTO 안 DTO 1:1 Select 하기 -->
	<resultMap type="tpm.study.model.Car" id="Car">
		<!-- column : db 의 컬럼명 -->
		<!-- property : dto 의 멤버 변수명 -->
		<result property="ownerId" column="ownerId"  />
		<result property="modelNo" column="modelNo"  />
		<result property="color" column="color"  />
	</resultMap>

	<resultMap type="tpm.study.model.Person" id="Person">
		<result property="id" column="id"  />
		<result property="name" column="name"  />
		<result property="age" column="age"  />
		<collection property="car" resultMap="Car" />
	</resultMap>

	<select id="dtoStudy1Select" resultMap="Person">
		SELECT p.id, p.name, p.age,
		c.ownerid, c.modelno, c.color
		FROM person p
		JOIN car c
		ON p.id = c.ownerid
	</select>
	<!-- -->
	<!-- Mybatis DTO 안 DTO 1:N Select 하기 -->
    <resultMap id="memoResultMap" type="tpm.study.model.MemoDTO">
        <id property="idx" column="idx"/>
        <result property="subject" column="subject"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="w_day" column="w_day"/>
        <result property="memo" column="memo"/>
        <collection property="comments" javaType="java.util.List" resultMap="commentResultMap"/>
    </resultMap>
    
    <resultMap id="commentResultMap" type="tpm.study.model.CommentDTO">
        <id property="idx" column="idx"/>
        <result property="memo_idx" column="memo_idx"/>
        <result property="c_name" column="name"/>
        <result property="c_email" column="email"/>
        <result property="c_w_day" column="w_day"/>
        <result property="comm" column="comm"/>
    </resultMap>
    
    <!-- <resultMap id="getMemo_comm" type="tpm.study.model.MemoDTO" extends="getMemo">
        <result property="comments" select="getCommList"  column="{memo_idx=idx}"/>
    </resultMap> -->
	
	
	<select id="dtoStudy2Select" resultMap="memoResultMap">
        SELECT * FROM memo_T m LEFT OUTER JOIN comment_t c ON m.idx = c.memo_idx
    </select>
    
		
</mapper>
